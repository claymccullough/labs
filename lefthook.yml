pre-commit:
  parallel: true
  jobs:
    - name: ruff-lint
      glob: "*.py"
      run: uv run ruff check --fix {staged_files}
      stage_fixed: true
    - name: ty-check
      glob: "*.py"
      run: uv run ty check
